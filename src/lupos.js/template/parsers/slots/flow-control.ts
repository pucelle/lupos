import type TS from 'typescript'
import {SlotBase} from './base'


export class FlowControlSlot extends SlotBase {

	init() {

	}

	outputInit(): (TS.Statement| TS.Expression)[] {
		
	}

	outputUpdate(): (TS.Statement| TS.Expression)[] {
		
	}
}