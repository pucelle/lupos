{
	"name": "@pucelle/lupos.compiler",
	"version": "1.0.0",
	"description": "A Typescript Transformer helps to compile lupos.js based projects.",
	"author": "pucelle",
	"license": "MIT",
	"homepage": "https://github.com/pucelle/lupos.compiler",
	"repository": {
		"type": "git",
		"url": "https://github.com/pucelle/lupos.compiler.git"
	},
	"main": "./out/index.js",
	"scripts": {
		"build": "cd compiler && tsc -b && cd transformer && tsc -b",
		"build-compiler": "cd compiler && tsc -b",
		"build-transformer": "cd transformer && tsc -b",
		"watch-compiler": "cd compiler && tsc -b -w",
		"watch-transformer": "cd transformer && tsc -b -w",
		"prepublish": "npm run build",
		"test": "cd tests && node ../bin/tscl.js && echo Compare git changes to get test result"
	},
	"bin": {
		"tscl": "./bin/tscl.js"
	},
	"devDependencies": {
		"typescript": "^5.6.2"
	}
}
